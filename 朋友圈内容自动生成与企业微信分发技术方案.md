# 朋友圈内容自动生成与企业微信分发技术方案

## 1. 结论先行

当前项目已经具备一条以 PostgreSQL 为持久化事实源、以 MinIO 保存私有素材、以
scheduler/worker 分工执行的内容生产链路。企业微信分发应作为“素材包完成”之后的独立
副作用阶段接入，不应改写选题、文案验证、图片幂等和人工复核的结果。

推荐路线：

1. 继续使用现有 acquisition、governance、content 三组服务，不引入 Redis、Celery、RQ
   或第二个向量数据库。
2. 新增独立的 wecom-dispatcher 进程，沿用现有 worker 的数据库轮询、短事务抢占、
   lease、heartbeat、有限重试和结构化日志模式。
3. 发送对象只允许是企业微信组织内已经配置的员工；第一版支持一个固定员工，之后再
   扩展到多个收件人。
4. 默认先由前端人工确认素材包，再创建正式分发任务。验证通过、审计通过和图片成功是
   正式分发的硬前置条件。
5. 可以通过显式配置开启“生成完成后自动发给固定员工”，但这只代表发送企业微信内部
   消息，不代表自动发布到员工的个人朋友圈。

本方案解决的是“把已生成的文案和图片发给指定的人”，不解决也不尝试解决个人微信
朋友圈的自动发布。

## 2. 与现有项目的对应关系

现有生产链路：

    acquisition-scheduler
      -> acquisition-worker
      -> PostgreSQL/MinIO 中的来源、快照、证据和事件
      -> governance-scheduler/governance-worker
      -> topic-selection（按业务日期的不可变版本）
      -> content-scheduler
      -> content-worker（文案生成、确定性验证、最多一次修复、LLM 审计）
      -> image artifact（图片模型、MinIO 私有对象、图片请求指纹）
      -> material package（选题、文案、图片、来源、证据、品牌和审计版本）
      -> 前端人工查看、复制文案、下载图片/素材包

企业微信接入只新增最后一段：

    material package
      -> wecom_delivery_job
      -> wecom-dispatcher
      -> 企业微信应用消息：文字 + 图片
      -> 指定内部员工

各层职责保持清晰：

| 层/服务 | 继续负责的内容 | 企业微信接入后的变化 |
| --- | --- | --- |
| acquisition | 抓取、来源限额、10 天新鲜度、快照和证据 | 不直接调用企业微信 |
| governance | 事实分析、证据绑定和事件组织 | 不直接调用企业微信 |
| topic selection | 每日选题和同日不可变修订 | 不因发送失败重算选题 |
| content-worker | 文案、验证、审计、图片任务的生产 | 只创建分发任务，不持有企业微信 token |
| API | 查询和人工操作 | 增加收件人、创建分发、查询状态接口 |
| MinIO | 私有原图、图片和素材包 | dispatcher 通过内部存储适配器读取图片 |
| PostgreSQL | 所有运行、版本、幂等和审计状态 | 增加分发任务、尝试和交互记录 |
| wecom-dispatcher | 新增 | token、临时图片素材、文字/图片消息、重试和投递审计 |

## 3. 不引入第二套队列

现有 worker 已经通过 PostgreSQL 完成任务持久化、抢占和 lease 恢复。企业微信分发是
一个低并发、可审计的内部消息出口，使用同样的模式即可：

    API/worker 短事务：创建唯一 delivery job
    dispatcher 短事务：SELECT ... FOR UPDATE SKIP LOCKED 抢占任务
    外部调用：在事务外获取 token、上传图片、发送消息
    dispatcher 短事务：保存安全结果并推进状态

这样可以保证服务器重启后任务仍在数据库中，不依赖某个进程的内存，也避免为了单个
收件人增加 Redis 运维、队列监控和新的故障面。未来确实出现大量并发收件人时，再以
测量结果决定是否拆分消息队列。

## 4. 企业微信接入边界

### 4.1 第一版接入

在企业微信管理后台创建一个自建应用，配置：

- 企业 CorpID；
- 应用 AgentID；
- 应用 Secret；
- 目标员工的 userid 和应用可见范围；
- 应用消息权限。

第一版只做应用向员工发送消息，不做企业微信回调、不做卡片按钮、不做公网 H5。
这样发送路径是单向的，部署不需要新增公网入口，也不需要在当前 API 外再暴露一个
回调服务。

### 4.2 后续交互

需要“重新生成文案”“重新生成配图”“预览”“发送测试”等按钮时，再增加模板卡片或
受保护 H5。新增交互必须满足：

- 回调入口使用可信 HTTPS；
- GET/POST 回调都验证企业微信签名并解密 EncodingAESKey；
- 任务 ID 和操作 token 绑定员工身份、操作类型、过期时间和一次性 nonce；
- 回调只做验签、鉴权和创建内部任务，不在请求中同步调用模型；
- 数据库保存重放结果，重复点击只返回原任务状态；
- H5 不把 MinIO signed URL、企业微信 token 或模型密钥放到浏览器。

模板卡片和 H5 是操作入口，不是绕过人工确认或自动发布朋友圈的入口。

## 5. 消息发送流程

### 5.1 正式发送

正式发送只允许引用一个已经版本化的素材包，不重新拼接选题、文案或图片：

1. API 检查素材包属于已接受的文案运行，确定性验证通过，审计 accepted，图片 artifact
   为 succeeded。
2. API 根据 recipient_id 创建或复用唯一的 wecom_delivery_job。
3. dispatcher 抢占任务并在内存中获取或复用 access_token。token 提前刷新，不落库、不
   写日志。
4. dispatcher 使用 MinIO 内部客户端读取私有图片，执行大小、媒体类型和 PNG/JPEG/WebP
   校验。
5. 调用企业微信临时素材接口上传图片，获得本次发送用的 media_id。media_id 只在本次
   job 内短期使用，不能替代 MinIO 的永久素材记录。
6. 向同一 userid 发送文字消息，再发送图片消息；每一种消息单独记录结果。
7. 文字和图片都成功后，delivery job 变为 delivered；只成功一部分时变为 partial，
   后续只重试未成功的消息。
8. 保存企业微信 errcode/errmsg 的安全映射、发送时间、消息类型、尝试次数和 provider
   request id；原始响应正文不进入日志或前端。

文字和图片分成两个消息是第一版的可观测实现。若后续改为图文消息，必须先在测试应用
验证媒体生命周期、正文长度和展示效果，再更换 delivery message kind，不影响已有素材包。

### 5.2 测试发送

test 模式必须和 formal 模式分开：

- test 的幂等键包含 test、操作者和目标收件人；
- 消息正文带有“测试消息”标识；
- test 不改变素材包审核状态，不算正式投递成功；
- test 也必须记录完整尝试和错误码；
- 测试按钮不能把一个失败的正式 job 静默改成成功。

### 5.3 自动发送开关

推荐初始配置：

    WECOM_ENABLED=true
    WECOM_AUTO_DELIVERY_ENABLED=false
    WECOM_REQUIRE_REVIEW_BEFORE_SEND=true

需要每天自动发送给一个固定内部员工时，打开 AUTO_DELIVERY，并且只允许配置中的
DEFAULT_RECIPIENT_ID。自动发送触发条件仍然是：

    copy run = accepted
    validation.passed = true
    audit.accepted = true
    image artifact = succeeded
    recipient = enabled

自动发送失败不会重新抓取新闻、重新选题或篡改素材包，只会把分发 job 置为可重试或
终态失败，并告警给管理员。

## 6. 幂等、重试和不确定结果

### 6.1 幂等键

建议使用稳定指纹：

    stable_key(
      "wecom-delivery",
      material_package_id,
      recipient_id,
      delivery_mode,
      package_version,
    )

同一个素材包、同一个收件人和同一种模式只允许一条正式 delivery job。重新生成文案或
图片会产生新的 copy run/material package，不能覆盖旧 job。

每个消息子任务还保存：

- message_kind：text 或 image；
- request_fingerprint；
- attempt_number；
- provider request id；
- sent_at；
- 安全的错误码；
- 是否可能已经送达。

### 6.2 错误分类

| 情况 | 处理 |
| --- | --- |
| token 过期或无效 | 清除内存 token，刷新一次后重试当前调用 |
| HTTP 超时且服务端结果未知 | 标记 delivery_unknown，禁止盲目重复发送，等待人工核对 |
| 企业微信限流或临时不可用 | 指数退避加 jitter，最多有限次数 |
| userid 不存在、应用不可见、权限不足 | 终态失败，提示修正后台配置 |
| 图片格式/大小不符合企业微信要求 | 终态失败，保留素材包，不重新生成文案 |
| 文字成功、图片失败 | partial，只补发图片 |
| dispatcher 宕机或 lease 过期 | 任务可被另一实例重新领取；重领前检查已有子任务状态 |

只有明确的 token 刷新、限流和临时网络故障可以自动重试。网络超时导致的“服务端是否
已经接收”属于未知结果，不能靠无限重试解决重复消息问题。

## 7. 数据模型

沿用现有 material_packages、copy_generation_runs、copy_draft_versions 和
image_artifacts，不新增 content_jobs/copy_versions/image_versions 这类重复实体。

建议新增表：

### wecom_recipients

- id：UUID；
- display_name：内部显示名；
- userid_ciphertext：加密后的企业微信 userid；
- userid_hash：用于唯一性和查重的盲化哈希；
- enabled：是否允许发送；
- created_at、updated_at；
- 禁止在列表 API 返回 userid_ciphertext 或原始 userid。

### wecom_delivery_jobs

- id、material_package_id、recipient_id；
- mode：test 或 formal；
- status：queued、running、delivered、partial、delivery_unknown、failed、cancelled；
- package_version、content_fingerprint、request_fingerprint；
- text_status、image_status；
- lease_owner、lease_token、lease_expires_at、heartbeat_at；
- attempt_count、next_attempt_at、last_error_code；
- created_at、started_at、completed_at。

唯一约束建议为：

    (material_package_id, recipient_id, mode, package_version)

如果允许同一个素材包对同一员工重新发一条正式消息，必须由新的 delivery revision
和明确的人工操作创建，不能通过普通重试绕过唯一约束。

### wecom_delivery_attempts

每一次外部调用一行，保存 message_kind、attempt_number、request_fingerprint、
provider_request_id、safe_response_code、result_state、latency_ms、created_at。禁止保存
Authorization header、access_token、原始响应、media_id 的长期副本和图片 URL。

### wecom_interactions

作为后续卡片/H5 的审计表，保存 delivery_id、recipient_id、action、request_id、nonce_hash、
result_state 和 created_at。原始回调 XML/JSON 只在有明确保留策略的安全审计存储中保存，
默认不写普通应用日志。

所有表均使用 Alembic 迁移，外键指向现有素材包，迁移不调用企业微信 API。分发状态和
尝试记录由 PostgreSQL 负责，不用 JSON 日志作为状态源。

## 8. API 设计

API 仍由现有 acquisition-api/FastAPI 提供，分发器不直接接受浏览器请求：

    GET  /api/v1/wecom/recipients
    POST /api/v1/material-packages/{package_id}/wecom-deliveries
    GET  /api/v1/wecom-deliveries/{delivery_id}
    POST /api/v1/wecom-deliveries/{delivery_id}/retry

创建请求建议字段：

    {
      "recipient_id": "uuid",
      "mode": "test" | "formal",
      "include_copy": true,
      "include_image": true
    }

要求：

- 只接受 recipient_id，不接受前端直接传 userid；
- formal 请求需要管理员身份或已批准素材包；
- 自动发送只从固定收件人配置创建；
- 返回 202、delivery_id、状态 URL 和安全的下一步提示；
- status API 只返回业务状态、消息类型结果和安全错误码；
- retry API 只能重试可重试/人工确认后的任务，不能重发已经 delivered 的消息；
- 所有写接口支持请求 ID 和幂等键，错误使用现有 problem response。

不新增以下接口：

    /publish
    /朋友圈自动发布
    /login-capture

## 9. 企业微信适配器

适配器只放在 infrastructure/外部客户端边界，application 层只依赖 provider-neutral
端口：

    WeComTokenProvider
    WeComMediaUploader
    WeComMessageSender

dispatcher 组合这些端口，应用层不依赖 httpx、企业微信 URL 或 SDK。适配器内部统一做：

- HTTPS、连接/读取/总耗时限制；
- JSON schema 和 errcode 校验；
- CorpID/AgentID/Secret 不出现在异常文本；
- 401/403/限流/5xx/网络超时的类型化映射；
- URL host allowlist，仅允许企业微信 API host 和 MinIO 内部客户端；
- 请求/响应大小上限；
- 发送前校验 message kind、正文长度、图片 media type 和 bytes。

access_token 使用进程内短期缓存，按 CorpID/AgentID 分组，过期前刷新。多 dispatcher
实例时，单实例重复取 token是可接受的；若实测企业微信限额成为瓶颈，再增加带最小权限
的加密 token cache。

## 10. 配置和密钥

配置示例只写占位符，真实值放服务器 secret store 或权限为 600 的 .env：

    WECOM_ENABLED=false
    WECOM_CORP_ID=
    WECOM_AGENT_ID=
    WECOM_CORP_SECRET=
    WECOM_DEFAULT_RECIPIENT_ID=
    WECOM_AUTO_DELIVERY_ENABLED=false
    WECOM_REQUIRE_REVIEW_BEFORE_SEND=true
    WECOM_REQUEST_TIMEOUT_SECONDS=15
    WECOM_MAX_ATTEMPTS=3
    WECOM_TOKEN_REFRESH_SKEW_SECONDS=300
    WECOM_CALLBACK_TOKEN=
    WECOM_ENCODING_AES_KEY=

日志、前端、OpenAPI 响应、异常和数据库普通字段都不得出现 CorpSecret、回调密钥、
access_token、Authorization header、原始 userid、MinIO object key、signed URL 或图片
原始内容。MinIO 中的图片仍然保持 private，企业微信只拿到本次发送的临时 media_id。

## 11. Compose 部署

第一版 Compose 保持现有服务：

    postgres + minio + minio-init + backend-migrate
    acquisition-api + acquisition-scheduler + acquisition-worker
    governance-scheduler + governance-worker
    content-scheduler + content-worker

增加 wecom-dispatcher 后：

- dispatcher 使用与 content-worker 相同的 backend 镜像和私有网络；
- dispatcher 只挂载必要的配置，不挂载源码；
- dispatcher 不暴露宿主机端口；
- dispatcher 依赖 backend-migrate 成功、PostgreSQL healthy 和 MinIO healthy；
- API、scheduler、content-worker、dispatcher 使用同一套 migration head；
- docker compose --profile governance --profile content up -d --build 是完整开发栈
  启动命令；
- 生产发布先执行 migration，再滚动重建 API/worker/dispatcher，保留 PostgreSQL 和
  MinIO volume；
- 不执行删除 volume 的 down 命令作为发布步骤。

当前项目没有公网反向代理。没有回调/H5 的第一版不需要开放公网端口；如果进入交互阶段，
另行部署 Nginx/Caddy，只开放 HTTPS 443，把 /wecom/callback 转发到 API。

## 12. 分阶段落地

### 阶段 0：先验收生成链路

- 保证来源抓取、治理、选题和 content worker 都是绿色；
- 检查当天选题使用最新未 supersede 的 revision；
- 检查 copy run 为 accepted，validation 和 audit 均通过；
- 检查 image artifact 为 succeeded，图片已写入 MinIO；
- 图片模型认证失败时只保留安全错误码并告警，不能用假图冒充真实图片。

本次验收发现 ToAPIs 返回 provider_authentication_failed。该问题属于 ToAPIs 凭据/
权限配置，不应通过代码绕过。当前版本的失败图片 reservation 仍然保留原始请求指纹，
但没有公开的人工 retry-image 接口；不能直接改数据库重试。企业微信接入前应补一个受
管理员保护的 retry-image 操作：只允许 failed/review_required 的图片 reservation 回到
queued，沿用同一请求指纹，成功结果仍只保存一份；已 succeeded 的图片禁止重试。

### 阶段 1：单收件人、单向消息 MVP

- 新增 recipient 和 delivery 表、Alembic 迁移；
- 先补齐受保护的 retry-image API/管理操作，避免图片供应商短暂故障后只能人工改库；
- 先实现 token、临时图片上传、文字消息、图片消息；
- 通过管理 API 手动创建 test job；
- test 成功后再创建 formal job；
- 不做回调、不做卡片、不做自动发布。

### 阶段 2：固定员工自动分发

- 配置一个启用的 recipient；
- 开启 WECOM_AUTO_DELIVERY_ENABLED；
- 只对满足生成前置条件的最新素材包创建 formal job；
- formal job 同样保留完整审计和人工可见状态；
- 图片或企业微信失败不影响选题、文案和素材包版本。

### 阶段 3：运营交互

- 增加模板卡片或受保护 H5；
- 加入员工身份、一次性 token、重放保护；
- 支持从企业微信触发重生成，但重生成必须创建新版本；
- 发送测试和正式发送始终分开；
- 增加管理员告警、投递统计和失败核对页面。

## 13. 测试与验收

### 单元/契约测试

- token 缓存、提前刷新、401 刷新一次；
- media upload 的图片大小、类型和错误响应；
- text/image 两种消息的 schema、长度和敏感字段脱敏；
- 429、5xx、连接超时的有限重试；
- 未知超时进入 delivery_unknown，不重复发送；
- 无效 userid、权限不足和图片校验失败进入终态；
- 同一 job 重放不会再次发送已经成功的子消息。

### PostgreSQL 集成测试

- delivery job 唯一约束和并发抢占；
- lease 过期后的恢复；
- 同一素材包重复创建只得到同一个 job；
- text 成功/image 失败后只补发 image；
- retry 不改变素材包、copy run、image artifact 的版本；
- migration clean upgrade、旧数据库 upgrade 和当前 head 检查。

### API/前端测试

- 收件人列表不返回 userid；
- formal 不能发送未通过验证/审计或图片未成功的素材包；
- 202 enqueue、状态轮询、错误码映射和幂等重放；
- 页面明确显示“发送给企业微信内部员工”，不出现“发布朋友圈”按钮；
- 状态包含 queued、running、partial、delivery_unknown、delivered、failed；
- API 合同生成后 frontend generated types 无 drift。

### 真实企业微信验收

使用一个专门的测试员工和测试应用：

1. 发送纯文本，确认 userid、AgentID 和应用可见范围；
2. 发送图片，确认临时 media_id 可用且 MinIO 没有被公开；
3. 发送同一 formal job 两次，确认只有一份有效投递记录；
4. 模拟 dispatcher 重启、429 和请求超时；
5. 检查日志、数据库普通查询、前端和错误页不存在 secret、token、原始 userid 或
   signed URL；
6. 员工收到后自行复制文案、保存图片并发布个人朋友圈。

真实企业微信测试不自动发布朋友圈，也不使用个人微信账号、Cookie、二维码登录或
自动化点击。

## 14. 运维和告警

至少监控：

- queued/running job 数量和最老任务年龄；
- delivered、partial、delivery_unknown、failed 数量；
- token 获取失败、限流次数、网络超时次数；
- text/image 分项成功率和延迟；
- 企业微信错误码按 recipient/app 的聚合；
- MinIO 读取失败和图片 hash/尺寸校验失败。

日志只记录 delivery_id、package_id、recipient_id 的内部 UUID、message_kind、attempt、
状态、错误码、耗时和安全 provider request id。告警通知也不能带文案正文、图片 URL 或
企业微信密钥。

## 15. 最终边界

完成本方案后，自动化能达到：

    自动抓取新闻
      -> 自动治理、选题、生成文案和图片
      -> 自动验证、审计、最多修复一次
      -> 组装版本化素材包
      -> 按规则自动或人工触发企业微信内部消息
      -> 指定员工查看、复制和自行发布

自动化不能也不应达到：

    自动登录个人微信
    自动操作朋友圈发布按钮
    使用 Cookie、二维码或非官方接口代发个人朋友圈

这条边界既保留系统全链路自动生产和内部送达能力，也让最终对外发布动作始终由员工
本人完成。
