# 公众号本地自动化与视觉优化 V2：实施计划

## Phase 0 — protection and baselines

- [x] Read task artifacts and backend/frontend/gzh specs; inspect `git status` and focused diffs before every
      high-collision edit.
- [x] Freeze V1 constants, golden hashes and current focused-test baseline; record that default external calls are 0.
- [x] Inventory existing media quality projections and choose the smallest fail-closed V2 gate without schema/migration.

## Phase 1 — V2 domain

- [x] Add explicit V2 identity/models for release, recipe, semantic emphasis, context block placement, content/artifact
      fingerprints and mobile binding. Leave V1 definitions/dispatch unchanged.
- [x] Implement deterministic semantic span selection and exact text round-trip tests.
- [x] Implement context-to-block scoring/fallback/collision spacing and tests for body+context interleaving.
- [x] Implement Xiaosai recipe selection, title/TOC length bands and callout variants using only allowlisted inline CSS.
- [x] Extend V2 preflight for release truth, placement validity, image separation, emphasis bounds and mobile binding.

## Phase 2 — application and artifact

- [x] Add injected release policy with `manual_only` compatibility and `quality_auto` machine release; manual rejection
      wins. Reuse durable validation/audit/media states and construct no provider clients.
- [x] Build V2 body/release/placement/theme/preflight/manifest and deterministic ZIP around content/artifact identity.
- [x] Support canonical runtime `not_run` and exact passed-report local export without fingerprint circularity.
- [x] Add offline application tests for gate matrix, determinism, tamper failure, ZIP safety and zero external clients.

## Phase 3 — API/OpenAPI/workbench

- [x] Inspect overlapping diffs, then minimally extend settings, schema, route and capabilities.
- [x] Regenerate OpenAPI/types; map only generated wire types into release/recipe/placement/mobile view models.
- [x] Update handoff UI and clipboard fallback/status copy; keep sandbox, accessibility and no-publish boundary.
- [x] Add API/mapper/component tests for automatic release, manual rejection, context placement and mobile identity.

## Phase 4 — news fixture, browser acceptance and export

- [x] Add deterministic local news fixture with >=3 body, >=1 context and 1 cover; preserve source/credit/rights warning.
- [x] Update Playwright to verify exact content/body/media hashes, order, image loading, 320/430 overflow and 0 external
      requests.
- [x] Produce a new non-overwriting local V2 export whose ZIP contains the passed fingerprint-bound report.
- [x] Run the installed gzh validator on final clean HTML to 0 ERROR/0 WARNING.

## Phase 5 — documentation and checks

- [x] Update English backend/frontend specs and operator docs for release policy, placement, recipes, report identity and
      permanent local/no-publish behavior.
- [x] Run focused Ruff format/lint, mypy, pytest, historical official-account regressions, API generation/drift,
      frontend format/lint/typecheck/Vitest/build, Playwright, Compose config where touched, and `git diff --check`.
- [x] Dispatch Trellis check agent, repair verified findings and rerun affected focused gates.
- [x] Report exact files/results/output path/external call count. Do not commit unless the user separately asks.

## Phase 6 — visible IP reference-conditioned body visuals

- [x] Freeze three exact body blocks and select matching approved Xiaosai/Sai Xiansheng reference publications; record
      truthful selection method, safe public refs, character labels and reference-input checksums.
- [x] Generate or adopt three validated 3:2 scenes whose visible protagonists include Xiaosai/Sai Xiansheng, then add a
      fail-closed local visual-map loader instead of reusing the generic fixture-body bytes.
- [x] Project the block -> brief/query -> reference -> generated-output lineage into safe handoff metadata/manifest and
      tests without exposing private paths, raw IDs, vectors, prompts or provider bodies.
- [x] Regenerate a fresh non-overwriting final bundle and exact mobile sidecar; visually inspect 320/430, run gzh, ZIP,
      V1/V2 regressions, types/lint/build and verify zero WeChat/WeCom calls.

## Phase 7 — weekly three-article local edition

- [x] Add a pure versioned weekly schedule and role-selection policy over already governed eligible candidates:
      official main article, industry trend and application case, with exact 7-day/14-day windows, stable fallback
      reasons, no duplicate events and no veto/threshold bypass.
- [x] Add a development-only weekly aggregate contract/writer that accepts exactly three finalized V2 artifacts,
      proves distinct child identities and passed/local-only/unpublished truth, preserves every child byte and emits a
      safe index, manifest and deterministic ZIP without constructing providers or social clients.
- [x] Add a zero-network fixture/CLI and focused tests for weekly due/catch-up boundaries, official priority/lookback,
      non-official fallback, category affinity, partial/duplicate/tampered child rejection, deterministic replay and
      atomic no-clobber output.
- [x] Export one fresh local weekly example containing three independent article directories; run focused Ruff,
      mypy, pytest, gzh/HTML and ZIP checks, task validation and `git diff --check`, then dispatch Trellis check.

## Phase 8 — homepage pin operator handoff

- [x] Add a strict role-to-display policy: official anchor is `pinned_primary` with a large-homepage-card cover
      purpose; industry and application remain `standard` with ordinary-thumbnail cover purposes. Bind every row to
      existing V2 cover path/hash/dimensions and keep WeChat crop/UI ownership explicit.
- [x] Add a typed, auditable local state machine and projection: immutable batch starts `not_published`, an explicit
      external publication confirmation produces `awaiting_manual_pin`, and only an explicit operator pin confirmation
      produces `confirmed`. Reject skipping, replay, time reversal and batch/official-article identity mismatch.
- [x] Add deterministic JSON/HTML/README/operator-checklist projections plus no-clobber post-export state sidecars;
      preserve all child bytes and avoid any WeChat/WeCom/provider/network adapter or call.
- [x] Extend the zero-network fixture and focused tests for exact three-role display policy, cover lineage, checklist
      steps, honest initial state, legal/illegal transitions, deterministic sidecars and child byte preservation. Run
      focused Ruff format/check, mypy, pytest, task validation and `git diff --check`; do not commit.

## Phase 9 — role-distinct weekly fixture images

- [x] Replace the shared weekly base media reuse with deterministic role-specific local compositions sourced from
      tracked science-scene assets and approved Xiaosai/Sai Xiansheng assets; keep 3:2 body, 2.35:1 cover, semantic alt,
      exact block/reference/output lineage and zero provider/network/social calls.
- [x] Make the weekly fixture independent of ignored historical `output/` directories and label its local context
      placeholders honestly without claiming live-news acquisition, Embedding, model or image-provider execution.
- [x] Fail closed in the aggregate on duplicate role cover bytes/pixels or duplicate ordered body-media hash/pixel sets;
      require all nine body hashes/pixels to be globally unique, and add decoded pixel/dimension/SHA plus metadata-only
      duplicate-child visual regression tests.
- [x] Export a fresh no-clobber weekly example and run focused Ruff, mypy, weekly plus V1/V2 pytest, ZIP integrity,
      320/430 local index validation, task validation and `git diff --check`; keep all external calls at zero.

## Phase 10 — opt-in live three-news local run

- [x] Add the exact three-role live-input contract and approved source registry; reuse the safe page/image fetchers and
      existing HTML/image extraction path with bounded, same-host, fail-closed provenance. Keep fixture/tests offline.
- [x] Build each child from its own fetched title/date/text/source/evidence/context images plus its role-specific local IP
      visual set; recompute all identities and reject all cross-role source, evidence, context or event reuse.
- [x] Add focused MockTransport tests for happy path, official authority, duplicate events, blocked/cross-host images,
      page/image failure and exact counters. Prove no model/Embedding/image-generation/WeChat/WeCom construction.
- [x] Execute one real three-source local run, export a fresh no-clobber batch and verify exact URLs/titles/dates/images,
      HTML/ZIP integrity, Ruff, mypy, focused pytest and task validation. Do not publish and do not commit.

## Phase 11 — one theme with three multi-source article clusters

- [x] Add a version-discriminated V2 live-input parser with one explicit theme, canonical official/policy,
      industry/method and application/practice clusters, and exactly one primary plus one supporting registered
      sources per cluster; preserve the complete V1 path.
- [x] Acquire every page/image through the existing safe fetchers, rebuild source-scoped claims/evidence/context media and
      reject global duplicate URL/page/event/evidence/image identities or cross-cluster leakage. Project theme, angle and
      primary/supporting provenance into child Article JSON/HTML, weekly index/manifest and a V2 live audit with exact
      variable counters.
- [x] Add zero-network MockTransport/resolver tests for V1 compatibility, valid V2 clusters, government-primary policy,
      missing/duplicate relations, global collisions, provenance containment and exact calls. Prove model, Embedding,
      image-generation, WeChat and WeCom calls/client construction remain zero.
- [x] Register and safely fetch a coherent six-or-more-source real acceptance manifest, export a fresh no-clobber local
      bundle, run exact 320/430 Playwright validation before aggregation, and verify GZH/HTML, images, ZIP, Ruff, mypy,
      focused regressions, task validation and `git diff --check`. Do not publish, overwrite earlier output or commit.

## Phase 12 — opt-in WeChat Official Account draft adapter

- [x] Add secret-safe, default-disabled WeChat MP settings and a typed application port/request/receipt contract. Inspect
      overlapping `.env.example` and config diffs first; keep the existing WeCom client and all local paths unchanged.
- [x] Implement a strict `api.weixin.qq.com` client for stable token caching, inline-image upload, permanent thumb upload
      and single-article `draft/add`, including bounded/duplicate-key-safe response parsing, typed redacted errors and one
      invalid-token refresh retry. Coalesce concurrent refresh of the same stale token and enforce the current official
      field/media limits without treating bare HTTP 401/403 as a token errcode. Do not implement `freepublish`, mass send,
      pinning or any real-call command.
- [x] Implement `draft_only` preparation/orchestration over finalized V2 article inputs: validate every local asset and
      exact HTML reference, upload filename, receipt clock and all three children before network; upload/rewrite body
      images, upload cover, create three independent drafts and return safe receipts without mutating source artifacts or
      advancing publication/pin state.
- [x] Add MockTransport contract/unit tests for exact request construction, independent article payloads, token cache/
      refresh, malformed/error/timeout paths, local path/hash/media tampering, zero secret/token leakage and proof that
      default tests construct no client. Run focused Ruff format/check, mypy, pytest, task validation and
      `git diff --check`; do not use real credentials or call WeChat.

## Validation commands

```bash
conda run --name edu-ai python -m ruff format --check <affected-python-files>
conda run --name edu-ai python -m ruff check <affected-python-files>
conda run --name edu-ai python -m mypy <affected-domain-and-application-files>
conda run --name edu-ai pytest backend/tests/unit/test_official_account_editor_handoff.py \
  backend/tests/unit/test_official_account_local_api.py -q
make api-generate
make api-contract-check
npm run test --prefix frontend -- --run src/features/official-account-local
npm run lint --prefix frontend
npm run typecheck --prefix frontend
npm run build --prefix frontend
npm run test:e2e --prefix frontend -- --grep "editor handoff"
python /root/.codex/skills/gzh-design/scripts/validate_gzh_html.py <clean-v2-body.html>
git diff --check
```

## Risky files and rollback points

- High collision: `.env.example`, `compose.yaml`, backend config/route/schema, `backend/openapi.json`, generated TypeScript,
  official-account Panel/api/hooks, README and Trellis specs. Inspect scoped diff first and never rewrite whole files.
- Prefer new V2 modules/tests and narrow wiring. No migration/repository/worker changes are expected.
- Rollback is release policy `manual_only`/V2 flag off; V1 remains intact.

## Pre-start gate

- [x] PRD/design/implement agree on additive V2, machine release truth and zero-WeChat boundary.
- [x] `implement.jsonl` and `check.jsonl` contain real spec/research entries.
- [x] `task.py validate` passes.
- [x] User approves the final planning summary after these artifacts are presented.
