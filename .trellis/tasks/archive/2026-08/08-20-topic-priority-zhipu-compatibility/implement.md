# Implementation Plan

1. [x] Add immutable Ministry priority v4 and a new current scoring version; preserve literal `.9`/v3 metadata, fingerprints and evaluation behavior.
2. [x] Implement the substantive science-education classifier using authenticated policy plus narrow policy/practice/talent/teaching signals, with stable event-only/promotion/homonym rejection reasons.
3. [x] Add rerank v4 minimal-order request/prompt/parser and local deterministic projection into existing rerank items; preserve literal v1/v2/v3 wire contracts.
4. [x] Keep complete-permutation, priority-barrier, event/version and automatic finalizer validation authoritative; route every invalid v4 output to the existing one-call deterministic fallback.
5. [x] Update Settings, `.env.example`, Compose and Doctor current defaults without changing content-disabled, downstream copy/image/OCR or delivery configuration.
6. [x] Add focused unit/provider tests for the real Tibet meeting regression, substantive positive cases, negative homonyms/promotions, historical replay, exact v4 payload, valid order and all malformed permutations.
7. [x] Add focused PostgreSQL/API audit coverage for new-version applied/fallback outcomes and historical replay; refresh provider-free eval artifacts and unchanged API contracts.
8. [x] Run Ruff, strict mypy, focused tests, migration-head/API/Compose/Doctor checks, provider-free eval, `make backend-check`, `git diff --check` and scoped secret/raw-response scans.
9. [x] After deterministic gates, perform at most one isolated `max_attempts=1` Zhipu compatibility probe with frozen synthetic candidates; record only typed result/usage and do not retry.
10. [x] Run independent Trellis review, fix blocking findings, update specs/result and record recurrence-prevention guidance.

## Boundaries

- Do not modify the user-owned report changes already present in the worktree.
- Do not fetch fresh news, replay the 2026-08-20 business run, create content/images/jobs, publish, SSH or deploy.
- Do not persist or log raw provider output, secrets, complete articles or private paths.
- A failed live compatibility probe is terminal for this task turn; deterministic fallback remains the product behavior.
